{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "env": {
          "NPM_FLAGS": "--legacy-peer-deps"
        }
      }
    }
  ]
}
